<%= bootstrap_form_with(model: sponsor, local: true) do |form| %>
  <% if sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sponsor.errors.count, "error") %> prohibited this sponsor from being saved:</h2>

      <ul>
      <% sponsor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.text_field :website %>
  </div>

  <div class="field">
    <%= form.text_field :blurb %>
  </div>

  <div class="field">
    <%= form.text_field :facebook %>
  </div>

  <div class="field">
    <%= form.text_field :twitter %>
  </div>

  <div class="field">
    <%= form.text_field :image_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
