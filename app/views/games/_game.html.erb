<%= link_to game.link, { class: "badge badge-pill badge-#{game.platforms&.first&.bootstrap_class || 'primary' }" } do %>
  <% game.platforms.map do |platform|  %>
    <%= icon(*platform.icon.split) if platform.icon %>
  <% end %>
  <%= game.name %>
<% end %>
