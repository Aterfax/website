# frozen_string_literal: true

require 'rails_helper'

RSpec.describe SocietyServer, type: :model do
  pending "add some examples to (or delete) #{__FILE__}"
  # it calls off to sleipnir
  # it brings back data
  # it tells how many players are on the server
  # it returns the current map (just accept a string response)
  # probably need some view tests to make sure that everything's displayed safely if the server can't be contacted
end
